/*package application;

public class Bot {
	public void turnoBot() {
	    // Valutazione delle carte del bot
	    Carta cartaDaGiocare = null;
	    int valoreCartaPiùAlto = -1;
	    for (Carta carta : manoBot) {
	        if (carta.getValore() > valoreCartaPiùAlto) {
	            valoreCartaPiùAlto = carta.getValore();
	            cartaDaGiocare = carta;
	        }
	    }

	    // Controllo se il tavolo è libero
	    if (cartaDaGiocare != null && tavolo.isEmpty()) {
	        // Gioca la carta con il valore più alto sul tavolo
	        tavolo.add(cartaDaGiocare);
	        manoBot.remove(cartaDaGiocare);
	        visualizzaCarte();
	    }
	}


}*/
